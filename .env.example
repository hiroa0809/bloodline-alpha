# === Database (SQLite) ===
SQLITE_DB_PATH=./backend/bloodline.db

# === Scrapy ===
DOWNLOAD_DELAY=3

# === Backend (FastAPI) ===
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# === Frontend (Next.js) ===
NEXT_PUBLIC_API_URL=http://localhost:8000
